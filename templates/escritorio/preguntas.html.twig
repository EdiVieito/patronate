{% extends 'base-panel.html.twig' %}
{% block main %}
 <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h1 class="h2">Preguntas sin responder</h1>
  </div>
      <div class="container mt-4 table-responsive">
      {% if SinResponder is empty %}
    No hay preguntas sin responder
{% else %}
        <table class="table table-striped table-sm mb-4">
          <thead>
            <tr>
            <th scope="">Curso</th>
              <th scope="col">Usuario</th>
              <th scope="col">Pregunta</th>
              <th scope="col">Acciones</th>
            </tr>
          </thead>
          <tbody>
          
          {% for pregunta in SinResponder %}
            <tr>
              <td><span class="px-2">{{ pregunta.Curso }}</span></td>
              <td>{{pregunta.User.email}}</td>
             <td>{{pregunta.Pregunta}}</td>
              <td>
              <a href="preguntas/actualizar/{{pregunta.id}}" class="btn btn-primary text-light">
              <i class="fas fa-reply"></i><span class="sm-hidden xs-hidden"></span>
              </a> 
              <a href="preguntas/eliminar/{{pregunta.id}}" class="btn btn-danger"><i class="fas fa-times"></i><span class="sm-hidden xs-hidden"></span></a></td>
            </tr>
            {% endfor %}
          </tbody>
        </table>
        {% endif %}
      </div>
 <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-5 pb-2 mb-3 border-bottom">
        <h1 class="h2">Preguntas</h1>
  </div>
  
      <div class="container mt-4 table-responsive">
      {% if preguntas is empty %}
    No hay preguntas
{% else %}
        <table class="table table-striped table-sm">
          <thead>
            <tr>
              <th scope="col">Curso</th>
              <th scope="col">Pregunta</th>
              <th scope="col">Acciones</th>
            </tr>
          </thead>
          <tbody>
          {% for pregunta in preguntas %}
            <tr>
              <td><span class="px-2">{{pregunta.curso}}</span></td>
              <td>{{pregunta.pregunta}}</a></td>
              <td>
              <a href="preguntas/actualizar/{{pregunta.id}}" class="btn btn-info text-light">
              <i class="fas fa-edit"></i><span class="sm-hidden xs-hidden"> Editar</span>
              </a> 
              <a href="preguntas/eliminar/{{pregunta.id}}" class="btn btn-danger"><i class="fas fa-trash-alt"></i><span class="sm-hidden xs-hidden"> Eliminar</span></a></td>
            </tr>
            {% endfor %}
          </tbody>
        </table>
      </div>
{% endif %}
     
{% endblock %}
